<!DOCTYPE html>
<html lang="tr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Node.js - Express.js</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="">
    <%- include('partials/navbar') %>
    <h1 class="my-5 text-4xl font-semiBold px-5">Home Page</h1>
    <main class="w-full flex flex-col gap-3 px-5">
      <% products.forEach(product => { %> <% if (product.isHome) { %> <%- include('partials/product', product) %> <% } %> <% }) %>
    </main>
  </body>
</html>
